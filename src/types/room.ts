export type Room = {
    id: number,
    tour_id: number,
    name: string,
    image_url: string,
    pitch: number,
    yaw: number,
    slug: string ,
    created_at: string,
    updated_at: string
  
}