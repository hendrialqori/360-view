export type Room = {
    id: string,
    tour_id: string,
    name: string,
    image_url: string,
    pitch: number,
    yaw: number,
    slug: string ,
    created_at: string,
    updated_at: string
  
}